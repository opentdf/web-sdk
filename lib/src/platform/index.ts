import { WellKnownService } from '../../proto/wellknownconfiguration/wellknown_configuration.pb.ts';

// List all static methods of WellKnownService
Object.keys(WellKnownService).forEach((key) => {
  console.log(key);
});